spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  main:
    allow-bean-definition-overriding: true

encryption:
  secret-key: testencryptionkey32bytestesttest
  salt: 0123456789abcdef
